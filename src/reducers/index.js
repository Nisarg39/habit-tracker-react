import handleChange from "./handleChange";
import { combineReducers } from "redux";

const rootReducer = combineReducers ({
    handleChange,
    // mention other reducers here
})

export default rootReducer;